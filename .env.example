# Zhipu / Z.AI API key for GLM-OCR — required for Phase 1 OCR.
# Get one at https://open.bigmodel.cn/usercenter/apikeys
ZHIPU_API_KEY=replace_me

# Optional: OCR request timeout in seconds (default 60).
# GLM_OCR_TIMEOUT=60
